function CreateJavaScript() {


    //retorno = retorno + "$.ajax({";
    //retorno = retorno + "   type: 'get',";
    //retorno = retorno + "   headers: {";
    //retorno = retorno + "       'Accept': 'application/json',";
    //retorno = retorno + "       'Content-Type': 'text/plain'";
    //retorno = retorno + "   },";
    //retorno = retorno + "   url: url,";
    //retorno = retorno + "   dataType: 'json',";
    //retorno = retorno + "   cors: true,";
    //retorno = retorno + "    async: true,";
    //retorno = retorno + "   crossDomain: true,";
    //retorno = retorno + "   data: {";
    //retorno = retorno + "         Dados: dados";
    //retorno = retorno + "      },";
    //retorno = retorno + "       success: function (result) {";
    //retorno = retorno + "          resultadoParametroExterno = result;";
    //retorno = retorno + "          callback();";
    //retorno = retorno + "     },";
    //retorno = retorno + "    error: function (result) {";

    //retorno = retorno + "    }";

    //retorno = retorno + "});";

    

    //containerID = "#" + containerID;
    //$(containerID).append(retorno);
}